name: CI

on:
  push:
    branches: [ master ]
    pull_request:
      branches: [ master ]

jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: echo Hello, world!